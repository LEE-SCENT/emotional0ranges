{
  "components": {
    "footer": {
      "$description": "색은 semantic(fg / fg-lower / border)을 그대로 씁니다 — Figma 도 여기서는 컴포넌트 색 토큰 없이 semantic 을 직접 바인딩합니다.",
      "snsIconSize": {
        "$type": "dimension",
        "$value": "{primitive.dimension}",
        "$extensions": { "scale": 5 },
        "$description": "SNS 아이콘 한 변. 20px."
      }
    }
  }
}
